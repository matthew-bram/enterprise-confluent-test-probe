# =============================================================================
# Test-Probe Java Quickstart - Helm Chart
# =============================================================================
# This Helm chart deploys the Java Quickstart example to Kubernetes.
# Use this as a SECONDARY option - Docker Compose is recommended for local dev.

apiVersion: v2
name: java-quickstart
description: Test-Probe Java Quickstart Example for Kubernetes
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  # Optionally depend on the localKafka chart
  # - name: localKafka
  #   version: "0.1.0"
  #   repository: "file://../../../localKafka"
